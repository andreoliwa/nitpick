{
  "name": "flake8-nitpick",
  "version": "0.13.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/andreoliwa/flake8-nitpick.git"
  },
  "release": {
    "plugins": [
      "@semantic-release/commit-analyzer",
      "@semantic-release/release-notes-generator",
      [
        "@semantic-release/changelog",
        {
          "changelogFile": "CHANGELOG.md"
        }
      ],
      [
        "@semantic-release/exec",
        {
          "prepareCmd": "echo bumpversion --allow-dirty --no-commit --no-tag --new-version echo{nextRelease.version} patch && echo pre-commit run --all-files end-of-file-fixer || true && echo rm -rf dist/ && echo poetry build && echo twine upload --verbose --password echoTWINE_TEST_PASSWORD -r testpypi dist/*",
          "publishCmd": "twine upload --verbose --password $TWINE_PASSWORD dist/*"
        }
      ],
      "@semantic-release/github",
      [
        "@semantic-release/git",
        {
          "assets": [
            "setup.cfg",
            "flake8_nitpick/__init__.py",
            "pyproject.toml",
            "package.json",
            "docs/conf.py",
            "nitpick-style.toml",
            "README.md",
            "CHANGELOG.md"
          ]
        }
      ]
    ]
  }
}
